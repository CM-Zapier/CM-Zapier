{
    "name": "cm-zapier",
    "version": "1.0.20",
    "description": "CM is a mobile messaging, voice, authentication and payment platform that enables you to engage with your target audience by mobile communication.",
    "repository": "https://github.com/CMTelecom/CM-Zapier",
    "homepage": "https://www.cm.com/",
    "author": "CM.com",
    "license": "CC-BY-NC-ND-4.0",
    "main": "src/index.js",
    "scripts": {
        "zapier-build": "tsc",
        "zapier-dev": "tsc",
        "zapier-push": "npm run zapier-build && zapier push",
        "prepare": "npm run zapier-build",
        "pretest": "npm run zapier-build",
        "test": "mocha --recursive build/src/tests && mocha --recursive build/lib/CM/tests && mocha --recursive build/lib/utils/tests && mocha --recursive build/lib/Zapier/tests"
    },
    "engines": {
        "node": "8.10.0",
        "npm": ">=5.6.0"
    },
    "dependencies": {
        "email-validator": "^2.0.4",
        "moment": "^2.22.2",
        "zapier-platform-core": "7.2.0"
    },
    "devDependencies": {
        "@types/mocha": "5.2.5",
        "@types/node": "10.5.7",
        "mocha": "5.2.0",
        "should": "13.2.3",
        "typescript": "3.0.1"
    }
}
